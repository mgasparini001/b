/*
1. It prints 0s in place of the two nums not filled in
2. It fails to compile because the over allocation "error C2078: too many initializers"
*/
#include <iostream>
#include <array>
using namespace std;
int main() {


	//array<int, 5> myArray1 = { 2,3,4 };

	array<int, 5> myArray2 = { 1,2,3,4,5,6 };
}